// src/index.ts

import { logger } from './utils/Logger';

logger.info('🚀 Baby Names Pipeline Started');
logger.info('Run: npm run dev');
logger.info('This will execute: migrate → download → import → sync')